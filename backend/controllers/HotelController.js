import Hotel from "../models/Hotel.js";

const createHotel = async (req, res) => {
    try {
        
    } catch(error) {
        return res.status(400).json({ message: "Something went wrong while creating hotel" });
    }
}